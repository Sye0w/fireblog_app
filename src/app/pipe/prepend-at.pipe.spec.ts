import { PrependAtPipe } from './prepend-at.pipe';

describe('PrependAtPipe', () => {
  it('create an instance', () => {
    const pipe = new PrependAtPipe();
    expect(pipe).toBeTruthy();
  });
});
